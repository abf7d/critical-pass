// import { Tag } from '@critical-pass/critical-charts';

import { Tag } from '@critical-pass/project/types';

export interface TagGroupOption {
    color: string;
    backgroundcolor: string;
    name: string;
    tags: Tag[];
}
